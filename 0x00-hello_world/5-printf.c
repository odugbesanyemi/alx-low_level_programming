#include <stdio.h>

/**
 * main - uses printsf to output a code 
 *
 * Return:  0 (success) 
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return(0);
}
